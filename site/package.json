{
  "name": "site",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build && pagefind --site dist",
    "preview": "node scripts/serve-dist.mjs",
    "astro": "astro",
    "test": "vitest run"
  },
  "dependencies": {
    "astro": "^7.0.6"
  },
  "devDependencies": {
    "pagefind": "^1.5.2",
    "vitest": "^4.1.10"
  }
}
